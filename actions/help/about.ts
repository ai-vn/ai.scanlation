import { ActionItem } from '~/actions/actions.type';

export const helpAbout: ActionItem = {
    call() {
        // Open Popup
    },
    title: 'About',
    accelerator: 'alt+i',
    icon: 'info',
};
